#! /usr/bin/env node

const { getToken } = require("../lib/token");
const token = getToken();

if (token) {
  console.log(token);
} else {
  console.error("Failed to generate token");
  process.exit(1);
}
